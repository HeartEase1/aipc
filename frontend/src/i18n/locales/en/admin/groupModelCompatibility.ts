export default {
  "listingOnly": "Model listing only",
  "listingHint": "Selected models only control the picker, without restricting requests. Preserved for older AIPC groups. Turn off to enforce the allowlist.",
  "blocked": "Denied models",
  "placeholder": "One model per line; paste to add multiple models",
  "blockedHint": "Denials always take precedence, even when the allowlist is off. A trailing * matches a prefix; names are case-sensitive. Leave empty for no additional denials."
}
